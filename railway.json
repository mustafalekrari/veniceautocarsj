{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn config.wsgi --log-file -",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
