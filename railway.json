{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py shell -c \"from django.contrib.auth.models import User; User.objects.filter(username='admin').exists() or User.objects.create_superuser('admin', 'admin@venice-autocars.ma', 'Admin2024!')\" && gunicorn config.wsgi --log-file -",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
